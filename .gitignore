__pycache__/
*.pyc
*.pyo
.DS_Store

# per-run scratch (the agent dumps its research sweep here before ingesting)
findings.json

# the persisted graph lives in ~/.cache/maya/graph/ — outside the repo

# kept local for now — the showcase site
build_site.py
docs/

# runtime narrative (the agent writes it per run)
log.md
